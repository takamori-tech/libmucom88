# libmucom88 の Codex 開発規約

C++17ヘッダーオンリーのMMLライブラリ。技術・API・検証の正本は [CLAUDE.md](CLAUDE.md)。
Codexが計画・実装・検証・自己レビュー・引き継ぎを担当する。Claude CLI・認証・PM・
`.claude/` の存在を必須にしない。旧モデル分担より本書を優先する。

## 作業の進め方

- `git status --short`、branchと直近commit、[session log](docs/session_log.md) の最新記録を確認する。
  Issue着手時は最新本文を確認する。過去のconsumer更新先・テスト数を現在の指示にしない。
- 目的・制約・完了条件を明確にし、許可済みのローカル作業を完了まで進める。
  小変更は直接作業、複雑な変更は計画、高影響変更は不変条件・consumer影響・検証を先に整理する。
  長い作業の計画と決定理由は関連docsに残す。新しいメッセージは現在の依頼への補足として扱う。
- 公開API、ヘッダーオンリー、例外不使用、音声パスのnoexcept/確保・ロック禁止を維持する。
  `include/mucom88/` がコア、`tests/` が単体テスト、`tools/` がCLI、`docs/` がAPI/組み込み契約。
- 原因を再現する既存テストを優先し、必要なら回帰テストを追加する。
  実装をなぞるテストや、根拠なく同じ全体検証を反復する手順を増やさない。
- 文書のみは `git diff --check` と参照・コマンド照合。C++変更は CLAUDE.md の
  CMake/CTest を実施し、MMLや音声挙動なら必要なconsumer回帰も確認する。
- 可逆な内部実装の詳細は判断して進める。要求や互換性を変える不明点だけ質問する。
  指示が作業を止める場合は根拠ファイルと該当規則を示し、依存しない作業を進める。
- 利用者の変更を保持し、一括addや破壊的なcleanを避ける。並列機能を前提にしない。
  委譲が要求された場合の追加編集担当はworktreeを分け、build/consumer/共有一時パスを競合させない。

## Code Review Rules

- CLAUDE.md「高難度レビュー手法」を対象に応じて適用する。
  メモリ境界、実時間性、同期・寿命、MML、公開APIの実際の欠陥・退行を確認する。
- 指摘にはfile:line、影響、再現・実行証拠・仕様またはコード経路を示す。
  多数決で指摘を取り下げず、証拠で裁定する。指摘ゼロも正常な結果。
- 通常は自己レビュー。独立レビューが利用者または個別仕様で要求された場合は
  新規Codexコンテキストを使い、実施できなければ未完了と記録する。
  正式レビューの修正往復は最大3回とし、未解決なら証拠と設計論点を整理する。
- Claude併用は利用者が明示依頼したレビューだけ。必要な規約・skillを読み込むよう依頼し、
  実装・ビルドを委譲しない。通常運用でClaude認証を確認しない。

## 完了・承認・引き継ぎ

- 要求と各完了条件を現行差分・実出力で照合する。必要なテストがskipなら検証未完了。
  コマンド、終了コード、件数/数値、ログ、未確認範囲を記録し、他担当の要約だけを証拠にしない。
- 公開・リリース・main merge・履歴改変・ライセンス・課金・他リポジトリ更新は
  既存の明示承認を確認する。Issue/PR投稿・コメント・pushも許可範囲のみ。
  承認待ちなら差分・検証・投稿案を先に用意する。無応答を承認にしない。
- Issue番号がある変更はcommitに含め、なければ発明せず依頼の目的を書く。
  commit/pushが依頼範囲ならremoteとの一致まで確認し、main mergeの承認と混同しない。
- 終了時は [session log](docs/session_log.md) に最新状態を短く記録する。
  複数セッションの未完了作業は目的、残る条件、実行証拠、次の一手を残す。
  過去の数値や次候補を常駐規約へ追加しない。反復した運用ミスに応じて規約を修正し、古い指示も除く。

## 根拠（2026-09-06公式ページ確認）

- [Codex best practices](https://learn.chatgpt.com/guides/best-practices): 簡潔な規約と目的・検証の明示。
- [AGENTS.md](https://developers.openai.com/codex/guides/agents-md): リポジトリ指示の探索。変更後は新規セッションで読み込む。
- [Model guidance](https://developers.openai.com/api/docs/guides/latest-model): 指示の矛盾を監査し、許可範囲で完遂、影響に応じた検証。

音源の合格値、最大3回、consumer境界は本プロジェクト固有の判断。モデル名・最大推論強度・
権限拡張を公式推奨として固定しない。利用環境の設定と利用者指定を尊重する。
