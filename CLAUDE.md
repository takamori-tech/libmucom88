# libmucom88 開発規約

本書は人とエージェントに共通の技術・検証規約です。
Codexの実行方法・承認・引き継ぎは [AGENTS.md](AGENTS.md)、利用者向けの入口は [README](README.md) です。
Claude CLI・認証・別プロジェクトへのアクセスは、コアの開発開始条件ではありません。

## 対象と正本

libmucom88はC++17ヘッダーオンリーのMMLパーサー・シーケンサーです。
`MmlParser → MmlEngine → IFmEngine → 音源バックエンド` の順で処理します。
コアにエミュレータ依存はなく、任意の `ymfm_engine.hpp` を使う場合だけymfm本体が必要です。
音声デバイス・GUI・スレッド間の受け渡しは利用側が担当します。

| 場所 | 正本とする内容 |
| --- | --- |
| `include/mucom88/` | 公開型・実装・既定値 |
| `CMakeLists.txt` | ツールと単体テストのビルド・登録 |
| `tests/` / `tools/` | 回帰の再現例 / `drumkit_gen` |
| [docs](docs/README.md) | API・組み込み・互換性の説明 |
| [session log](docs/session_log.md) | 直近の変更・証拠・再開状態 |

修正元は独立したlibmucom88リポジトリです。利用側のvendorコピーやsubmodule内を直接編集して済ませません。
過去の利用先一覧を、現在の全利用者の一覧や変更許可として扱いません。

## 変更の流れ

1. 依頼、作業状態、branch、直近commit、作業記録を確認する。Issue作業では最新の本文を読む。
2. APIや挙動を実装と照合し、必要な修正と対応文書を本リポジトリで変更する。
3. 本書の検証を変更範囲に応じて実施し、差分をレビューする。
4. 利用側の更新が承認範囲なら、到達可能なcommitへgitlinkを更新し、そのリポジトリの規約・テストに従う。
5. 検証したrevision、実行証拠、未確認事項を記録する。push依頼がある場合はremoteとの一致まで確認する。

利用側の検証スクリプトがsubmoduleを更新する場合は、indexのgitlinkと検証対象が一致することを先に確認します。
古いcommitへ戻して実行した結果を、新しいcommitの検証結果として扱いません。
文書だけの変更に利用側gitlinkの更新を必須にしません。

## C++と公開API

- コメント・説明は日本語。C++17、`#pragma once`、コアのヘッダーオンリー設計を維持する。
- 新規・変更コードは [C++規約](docs/cpp_coding_standards.md) に従う。
- エラー処理のために明示的なthrowを追加せず、戻り値で失敗を表す。標準コンテナ等の確保失敗まで
  ライブラリ全体で処理・保証しているとは記載しない。
- 公開シグネチャ・列挙値・構造体・既定値・戻り値の意味を保護する。変更時は利用側の影響と移行を確認する。
- 新規・変更する音声処理に確保・解放・throw・待機ロックを持ち込まない。render系の `noexcept` を維持する。
  既存のoptional ymfmはmutexとボイスRAMコピーを持つため、現状の実時間保証とこの開発方針を区別する。
- `MmlEngine` はスレッドセーフではない。getterも含め、共有アクセスと寿命を利用側で管理する。

## テスト

リポジトリのルートで実行します。

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release \
  -DLIBMUCOM88_BUILD_TOOLS=ON -DLIBMUCOM88_BUILD_TESTS=ON
cmake --build build --parallel 8
ctest --test-dir build --output-on-failure --no-tests=error
```

| 変更 | 必要な確認 |
| --- | --- |
| 文書のみ | `git diff --check`、リンク・コマンド・API例の整合性。ライブラリのビルド・回帰は `skip` と記録 |
| コアC++・CMake | 関連する既存テストと、最終差分で上記CMake/CTest |
| optional ymfm | 上記に加え、実際に使うymfm revisionとのcompile/linkと関連動作 |
| MML・音声挙動 | 上記に加え、必要な参照実装比較・利用側の回帰 |

テスト登録の正本はCMakeの `add_test` です。テスト0件を成功としません。
原因を再現する既存テストを優先し、必要なら回帰条件を追加します。
低影響の変更に実装をなぞるだけのテストを追加せず、合格後は新たな変更・懸念がなければ同じ検証を反復しません。
golden・期待値を成功させる目的だけで更新しません。

コア単体の成功をoptional adapter、全曲、MIDI、DAW、実音の成功へ広げません。
未実行は `skip`、不明は `Unknown`、実音・操作感は利用者確認まで `NOT_VERIFIABLE`。
必要な条件が未確認なら、実装済みと検証完了を分けて報告します。

## MML再現目標

Open MUCOM88のZ80ドライバーを使う参照経路との機能的な一致を目指します。
現在の一致保証ではなく、挙動変更時の開発目標です。
処理構造と検証範囲は [互換性と検証](docs/Z80_vs_MmlEngine.md) を参照してください。

全曲のfmgen参照比較を実施できる環境では、既存基準の30秒／曲で比較し、
`avgRMS >= 0.8` 条件を全対象曲で維持し、
目標は比0.95–1.05、Median約1.000とします。この合格値はプロジェクト固有の条件であり、
RMSだけで波形・ノート時刻の一致を証明しません。
Z80起動遅延やタイマー書き込みの違いは、測定条件と影響を明示して評価します。

比較ツール `muc_regtest` 等は本リポジトリに含まれません。利用側で実施するときは、
実装・参照・入力・設定・秒数・除外件数・ログを記録し、共有一時パスを使うツールを多重起動しません。
fmgen参照の数値とymfmの決定論goldenを混同しません。
ROM・楽曲等が不足する場合は未確認として残し、テスト省略オプションを全曲PASSとして扱いません。

## 高難度レビュー手法

実時間性・MML再現・並行性・寿命・公開契約の変更では次を適用します。別モデルやローカルskillは前提にしません。

- 入口から呼び出し経路を追い、値の範囲と到達条件を確認する。Releaseで消えるassertだけを入力防御としない。
- `advance()` / `renderMixed()` / バックエンド呼び出しの確保・解放・throw・待機ロックを確認する。
  `noexcept` やテスト成功だけで実時間安全性を証明したことにしない。
- 共有データの所有者、書き込み元、同期、寿命を示す。表示用途でも非atomicの競合を許容しない。
- 参照実装の対象revision、レジスタ処理、公開契約と照合し、再現可能な反証を優先する。
  根拠のない行番号・引用・件数を作らない。
- 公開型・enum・既定値・仮想関数の変更では利用側まで影響を確認する。
- 指摘にはfile:line、影響、再現・トレース・仕様根拠を付ける。仕様の採点に加え、実際の欠陥・退行を調べる。
- 通常は自己レビュー。独立レビューが明示必須なら自己レビューで代用しない。
  正式レビューの修正往復は最大3回、未解決なら証拠と設計論点を整理する。採否は多数決で決めない。

承認範囲と終了時の引き継ぎは [AGENTS.md](AGENTS.md#完了承認引き継ぎ) に従います。
